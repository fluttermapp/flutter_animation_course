/* AnimatedDefaultTextStyle(
              style: TextStyle(
                color: currentSugar > 3 ? Colors.black : Colors.white,
                fontSize: currentSugar > 3 ? 60 : 30,
                fontWeight: FontWeight.bold,
              ),
              duration: const Duration(milliseconds: 300),
              child: Text(currentSugar.toString()),
            ), */